name: test-github-action-dev

on:
  push:
  pull_request:
    branches:
      - develop
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy dev
        run: echo "deploy dev"